"use client";

// The real react-slick slider plus its stylesheets, isolated into one module
// so it can be pulled in as a single lazily-loaded chunk (see LazySlider).
import Slider from "react-slick";
import "slick-carousel/slick/slick.css";
import "slick-carousel/slick/slick-theme.css";

export default Slider;
